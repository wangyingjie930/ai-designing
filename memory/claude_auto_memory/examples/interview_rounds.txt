请评审 customer_profile_lookup 工具的参数契约。目标分为三步：核对参数需求、起草模型可见 Schema、完成最终契约检查。

当前需求：
- customer_id：必填字符串，不能为空，示例 C-1024。
- include_orders：可选布尔值，默认 false。
- max_orders：可选整数，范围 1 到 50，默认 10。

请先完成需求核对并给出初步评审结论。
---
需求核对结论已确认：max_orders 只在 include_orders=true 时生效。

同时补充一条团队长期约定：所有新增工具参数都必须提供中文 jsonschema_description，清楚说明用途、取值含义、约束或示例。

请继续起草模型可见的参数 Schema，并指出仍需注意的风险。
---
Schema 草案通过。请完成剩余契约检查，给出最终参数契约和上线前检查清单。

最后说明：以后在这个项目中新增工具参数时，需要遵守什么团队约定？